application:
  name: nexlayer-website
  pods:
    - name: blog
      image: ghcr.io/nexlayer/nexlayer-blog:latest
      path: "/blog"
      servicePorts: [3000]
      vars:
        NODE_ENV: "production"
        NEXT_TELEMETRY_DISABLED: "1"
